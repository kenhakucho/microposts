<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <dl>
        <h1>
          <%= @user.name %>
          <%= gravatar_for @user %>
        </h1>
        <div class="clearfix"></div>
        <div>
          <ul class="row-fluid">
            <dl class="span1">
              <%= link_to "following  #{@user.following_users.count}", 
              "#{@user.id}/followings" %>
            </dl>
           <dl class="span2">
              <%= link_to "follower  #{@user.follower_users.count}", 
              "#{@user.id}/followers" %>
            </dl>
          </ul>

          <table class="table">
              <tbody>
                  <tr>
                      <td>EMAIL</td>
                      <td><%= @user.email %></td>
                  </tr>
                  <tr>
                      <td>BIRTHDAY</td>
                      <td><%= @user.birthday %></td>
                  </tr>
                  <tr>
                      <td>AREA</td>
                      <td><%= JpPrefecture::Prefecture.find(@user.area).name if @user.area.present? %></td>
                  </tr>
              </tbody>
          </table>
        </div>
        <dt>PROFILE</dt>
        <dd><%= @user.profile %></dd>
        
      </dl>
    </section>
  </aside>
  <div class="col-md-8">
    <%= render 'follow_form' if logged_in? %>
    <% if @user.microposts.any? %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
    <% end %>
  </div>
</div>
